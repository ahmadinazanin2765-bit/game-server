{
  "name": "game-server-api",
  "version": "1.0.0",
  "description": "somtehing for run game on netlify",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step required'"
  },
  "devDependencies": {
    "netlify-cli": "latest"
  }
}
